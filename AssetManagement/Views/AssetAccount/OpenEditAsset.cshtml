
@{
    ViewBag.Title = "EditAsset";
}

<h2>EditAsset</h2>


<div id="showEditAssetGrid">

</div>

<script>

    $(function () {
       
        editAssetGrid();
        @*var clients  = @Html.Raw(ViewBag.data);

    
        var mytable = $('<table class="table table-striped table-condensed"></table>').attr({ id: "basicTable" });
        var row = $('<thead></thead>').appendTo(mytable);
        for(key in clients[0]){
            $('<th></th>').text(key).appendTo(row);              
        } 

        clients.forEach( function(d){
            var row = $('<tr></tr>').appendTo(mytable);

            for(key in d){
                $('<td></td>').text(d[key]).appendTo(row);              
            }           
        })
     
        mytable.appendTo("#editAsset");*@

    });




    function editAssetGrid() {

        var clients  = @Html.Raw(ViewBag.data);


        $("#showEditAssetGrid").jsGrid({

            width: "100%",
            height: "400px",
            sorting: false,
            paging: true,
            editing: false,
            data: clients,
            pageSize: 15,
            pageButtonCount: 5,
            rowClick: function(args) {

                window.location.href = '/AssetAccount/EditAsset?assetName=' + args.item["Asset Name"];
            },

            fields: [

                { name: "Institution Name", type: "text", width:150 },
                { name: "Asset Name", type: "text", width: 150 }


            ]
        });
    }


 </script>